apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ing
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: 'dev.contoso.net'
      http:
        paths:
          - path: /
            backend:
              service:
                name: aspnetapp1
                port:
                  number: 80
            pathType: Prefix